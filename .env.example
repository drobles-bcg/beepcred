# Server
PORT=3010
SESSION_SECRET=beepcred-local-dev-secret-change-in-prod
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5180

# Client (Vite) — leave empty in production (same-origin /api)
VITE_API_URL=http://localhost:3010

# Optional: plate photo insights (OpenAI vision). Never commit real keys.
# OPENAI_API_KEY=
# OPENAI_VISION_MODEL=gpt-4o-mini
# OPENAI_VISION_DETAIL=low
# DISABLE_AI_PLATE_INSIGHTS=
