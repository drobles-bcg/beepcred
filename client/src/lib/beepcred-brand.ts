import { toAbsoluteUrl } from '@/lib/helpers';

/** Horizontal wordmark + icon (`public/media/beepcred/logo-horizontal.jpg`). Swap in a true PNG with alpha for a transparent background when you have it. */
export const BEEPCRED_LOGO_HORIZONTAL = toAbsoluteUrl('/media/beepcred/logo-horizontal.jpg');
